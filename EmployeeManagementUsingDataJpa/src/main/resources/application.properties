spring.application.name=EmployeeManagement


#database connection properties
spring.datasource.url=jdbc:mysql://localhost:3306/cts
spring.datasource.username=root
spring.datasource.password=362218
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
 
 #hibernate properties
 #it will create the table and update the crud operations recommonded for 1st use create
 # 2nd tym we have to use update
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true 
# it will show the sql queries which is generated by the hibernate
spring.jpa.format-sql=true;
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
#we are telling jpa use hiberante as a orm tool and generate the query and dispaly 

server.port=2345
management.endpoints.web.exposure.include=*